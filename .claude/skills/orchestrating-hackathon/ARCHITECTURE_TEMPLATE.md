# ARCHITECTURE.md — Stage 2 Output Template

> This document is the source of truth for Stage 3 task generation.
> Interfaces defined here are considered frozen after Stage 2 is confirmed.
> Changes require explicit approval and must trigger a change-impact review.

---

## System Overview

> One paragraph describing the system end-to-end: what it does, who uses it,
> and how the major pieces connect.

---

## Module Boundaries

> Define each module. Each module must have exactly one owner.

### Module A — [Name]

- **Owner:** Teammate A
- **Responsibility:** (what this module does and nothing else)
- **Exposes:** (what other modules can call / import from this one)
- **Consumes:** (what this module needs from others)

### Module B — [Name]

- **Owner:** Teammate B
- **Responsibility:**
- **Exposes:**
- **Consumes:**

### Module C — [Name]

- **Owner:** Teammate C
- **Responsibility:**
- **Exposes:**
- **Consumes:**

---

## Data Flow

> Describe how data moves through the system step by step.
> Use numbered steps. Be concrete — name the module and the data shape at each step.

1. User does X → Module A receives `{field: type, ...}`
2. Module A calls Module B with `{...}`
3. Module B reads from DB and returns `{...}`
4. ...
5. Response reaches user as `{...}`

---

## Interface Contracts

> Lock every cross-module interface before task generation.
> These are not suggestions — they are contracts that must be implemented exactly as written.

### Contract 1: [Module A] → [Module B]

```
Method / Endpoint: POST /api/example
Request:
  {
    field1: string,
    field2: number
  }
Response (200):
  {
    result: string,
    id: string
  }
Error (4xx):
  { error: string, code: string }
```

### Contract 2: [Module B] → [Module C]

```
[Define similarly]
```

---

## Shared Schemas / DTOs

> Data shapes that more than one module must agree on.
> Copy these into each relevant task package — do not expect teammates to look them up.

```typescript
// Example:
interface UserRecord {
  id: string
  name: string
  createdAt: Date
}
```

---

## Directory Tree

```
project-root/
├── module-a/              # Owner: Teammate A
│   ├── ...
├── module-b/              # Owner: Teammate B
│   ├── ...
├── module-c/              # Owner: Teammate C
│   ├── ...
└── shared/                # Shared types, constants, utils (discuss ownership)
    ├── types.ts
    └── ...
```

---

## Ownership Map

| Folder / Module | Owner | Branch | Notes |
|-----------------|-------|--------|-------|
| `module-a/` | Teammate A | `feat/module-a` | |
| `module-b/` | Teammate B | `feat/module-b` | |
| `module-c/` | Teammate C | `feat/module-c` | |
| `shared/` | [Discuss] | — | Coordinate before editing |

---

## Branch Naming Convention

```
feat/<module-name>     # Primary feature branches
fix/<short-description>
```

Integration branch: `main` (or `dev` — specify which)

---

## Integration Assumptions

> What must be true for the system to work end-to-end.
> These are not implementation details — they are assumptions every teammate must accept.

- All modules share the same DB instance
- Auth is handled by Module ___ (others trust the token it produces)
- Environment variables follow the schema in `shared/.env.example`
- [Add more as needed]

---

## Mock Strategy

> What can be faked for demo day vs. what must be real.

| Component | Real or Mock | Notes |
|-----------|-------------|-------|
| [Auth] | Real | Judges will test login |
| [External API] | Mock | Too slow to integrate in time |
| [Email sending] | Mock | Log to console instead |

---

## Decision

> State the chosen architecture clearly. One paragraph.

---

## Rationale

> Why this structure over alternatives. Be specific about what makes it safe for parallel work.

---

## Rejected Options

| Option | Why Rejected |
|--------|-------------|
|        |             |

---

## Open Questions

- [ ] Question that must be resolved before or during Stage 3
- [ ] Question that is low-risk to defer

---

## Risks

| Risk | Owner | Mitigation |
|------|-------|-----------|
| Module A depends on Module B's contract being stable | Both | Contract locked at Stage 2 |
| [Other risk] | | |

---

## Fallback Plan

> If the primary architecture becomes unshippable mid-hackathon, what's the simplest
> version of this system that can still demo?

---

*Generated by `orchestrating-hackathon` — Stage 2 complete*
